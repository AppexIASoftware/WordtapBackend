import { VaultView } from "@/features/teacher-dashboard/views/vault-view";

export const metadata = {
  title: "Banco Unificado de Contenidos (Vault) — Wordtap Studio",
};

export default function Page() {
  return <VaultView />;
}
