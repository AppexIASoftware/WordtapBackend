import { GamificationView } from "@/features/teacher-dashboard/views/gamification-view";

export const metadata = {
  title: "Juegos & Gamificación — Wordtap Studio",
};

export default function Page() {
  return <GamificationView />;
}
